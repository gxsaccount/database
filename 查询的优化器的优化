重新定义关联表的顺序,用贪心算法决定
将外连接转化为内连接
使用等价变换规则（如5=5 and a>5 变为 a>5）
优化count，min，max(如min（主键）查找BTree最左端的记录即可，不用全局搜索)
预估并转化表达式为常数
覆盖索引扫描
子查询优化
提前终止查询（如LIMIT）
等值传播（在where，on或者using自居）
列表IN（）的比较，mysql将in中排序，用二分查找，快于多个or
mysql不支持并行查询
